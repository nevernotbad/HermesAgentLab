{
  "name": "hermes-agent",
  "version": "1.0.0",
  "description": "An AI agent with advanced tool-calling capabilities, featuring a flexible toolsets system for organizing and managing tools.",
  "private": true,
  "workspaces": [
    "apps/*",
    "ui-tui",
    "ui-tui/packages/*",
    "web"
  ],
  "scripts": {
    "postinstall": "echo '✅ Browser tools ready. Run: python run_agent.py --help'",
    "install:root": "npm install --workspaces=false",
    "install:web": "npm install --workspace web",
    "install:tui": "npm install --workspace ui-tui",
    "install:desktop": "npm install --workspace apps/desktop",
    "audit:root": "npm audit --workspaces=false",
    "audit:web": "npm audit --workspace web",
    "audit:tui": "npm audit --workspace ui-tui",
    "audit:fix:root": "npm audit fix --workspaces=false",
    "audit:fix:web": "npm audit fix --workspace web",
    "audit:fix:tui": "npm audit fix --workspace ui-tui",
    "desktop:check": "npm run typecheck --workspace apps/desktop && npm run test:desktop:platforms --workspace apps/desktop && npm run test:ui:packaging --workspace apps/desktop && npm run test:packaging --workspace apps/desktop",
    "desktop:package:win": "npm run desktop:check && npm run verify:install-source --workspace apps/desktop && cross-env HERMES_REQUIRE_CLEAN_BUILD=1 npm run dist:win:nsis --workspace apps/desktop && npm run test:desktop:bundle --workspace apps/desktop && npm run test:desktop:install:win --workspace apps/desktop",
    "desktop:package:portable:win": "npm run desktop:check && npm run verify:install-source --workspace apps/desktop && cross-env HERMES_REQUIRE_CLEAN_BUILD=1 npm run dist:win:portable --workspace apps/desktop && npm run test:desktop:app-bundle --workspace apps/desktop && npm run test:desktop:portable:win --workspace apps/desktop"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nevernotbad/HermesAgentLab.git"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/nevernotbad/HermesAgentLab/issues"
  },
  "homepage": "https://github.com/nevernotbad/HermesAgentLab#readme",
  "dependencies": {
    "@streamdown/math": "^1.0.2",
    "agent-browser": "^0.26.0"
  },
  "overrides": {
    "lodash": "4.18.1",
    "@assistant-ui/store": "0.2.13",
    "yauzl": "^3.3.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
